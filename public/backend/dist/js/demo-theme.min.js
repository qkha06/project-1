/*!
* Tabler v1.0.0-beta20 (https://tabler.io)
* @version 1.0.0-beta20
* @link https://tabler.io
* Copyright 2018-2023 The Tabler Authors
* Copyright 2018-2023 codecalm.net Paweł Kuna
* Licensed under MIT (https://github.com/tabler/tabler/blob/master/LICENSE)
*/


(function (factory) {
	typeof define === 'function' && define.amd ? define(factory) :
	factory();
})((function () { 'use strict';
    function changeTheme(selectedTheme = null) {
        var themeStorageKey = "theme";
        var defaultTheme = "light";
    
        if (selectedTheme && (selectedTheme == 'light' || 'dark')) {
            localStorage.setItem(themeStorageKey, selectedTheme);
        } else {
            var storedTheme = localStorage.getItem(themeStorageKey);
            selectedTheme = storedTheme ? storedTheme : defaultTheme;
        }
        if (selectedTheme === 'dark') {
            document.body.setAttribute("data-bs-theme", selectedTheme);
        } else {
            document.body.removeAttribute("data-bs-theme");
        }
    }
    changeTheme();

    document.addEventListener("DOMContentLoaded", function () {
        const changes = document.querySelectorAll('[data-theme]');
        changes.forEach(change => {
            change.addEventListener('click', () => {
                const dataTheme = change.getAttribute('data-theme');
                if (dataTheme) changeTheme(dataTheme);
            })
        });
    });
    document.addEventListener("DOMContentLoaded", function () {
        const selectAutos = document.querySelectorAll('select[autoselection]');
        if (selectAutos) {
            selectAutos.forEach((elt) => {
                const val = elt.getAttribute('value');
                elt.value = val;
            })
        }
    });

}));